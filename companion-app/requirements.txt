# Review UI (native desktop window via pywebview, not a browser tab)
nicegui[native]

# Config
pyyaml
python-dotenv

# review_ui.py's Settings view - round-trip YAML so editing a value
# there doesn't strip every explanatory comment out of
# source_weights.yaml/mood_weights.yaml (plain pyyaml.dump() can't
# preserve comments; ruamel.yaml can)
ruamel.yaml

# fetch/musicbrainz.py, fetch/discogs.py
requests>=2.31

# fetch/audio_model.py - discogs-maest inference
essentia-tensorflow
numpy

# fetch/llm_web_search.py
anthropic

# charts/billboard_tag.py (ported as-is from billboard-tag)
rapidfuzz>=3.6
billboard.py>=7.0
